# Speech Service 配置文件
# 复制本文件为 .env 并按需修改

# 服务监听地址和端口
SPEECH_SERVICE_HOST=0.0.0.0
SPEECH_SERVICE_PORT=8080

# 模型路径（默认指向项目根目录下的对应文件夹）
# SPEECH_SERVICE_CHECKPOINTS_DIR=../checkpoints
# SPEECH_SERVICE_VOICES_DIR=../voices
# SPEECH_SERVICE_WHISPER_MODEL_DIR=..
# SPEECH_SERVICE_WHISPER_MODEL_NAME=base

# 计算设备：cuda / cpu（默认自动检测）
# SPEECH_SERVICE_DEVICE=cuda

# TTS 精度（GPU 推荐开启 fp16）
# SPEECH_SERVICE_USE_FP16=true

# CORS 允许的来源（默认 *，即允许所有）
# SPEECH_SERVICE_CORS_ALLOW_ORIGINS=["*"]
